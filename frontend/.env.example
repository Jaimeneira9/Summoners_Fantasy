# Supabase — valores públicos (safe para commitear el .example, no el .env.local)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key   # clave pública, RLS protege los datos

# Backend FastAPI
NEXT_PUBLIC_API_URL=http://localhost:8000      # URL del backend (VPS en producción)
